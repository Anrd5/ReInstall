name: RefreshClones

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  set-apk-trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Bump APK trigger flag
        run: |
          set -euo pipefail
          mkdir -p triggers
          {
            echo "apk_trigger=1"
            echo "repo=${GITHUB_REPOSITORY}"
            echo "run_id=${GITHUB_RUN_ID}"
            echo "run_number=${GITHUB_RUN_NUMBER}"
            date -u +"timestamp=%Y-%m-%dT%H:%M:%SZ"
          } > triggers/apk.flag

      - name: Commit and push
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add triggers/apk.flag
          git commit -m "chore(trigger): APK run ${GITHUB_RUN_ID}"
          git push
