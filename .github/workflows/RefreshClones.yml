name: Refresh APKs

on:
  workflow_dispatch:

jobs:
  apk-update:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Termux manage.sh
        run: |
          chmod +x ~/manage.sh
          ~/manage.sh
